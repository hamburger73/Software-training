public class Plane extends Vehicle {
    public Plane(String type, String powerType, int numWheels, int numDoors, String color, double fuelEconomy, int passangerCapacity) {
        super(type, powerType, numWheels, numDoors, color, fuelEconomy, passangerCapacity)
    }
    // If landing gear is down
    public
    // Make a takeoff function
    // Make a landing function (only land if gear is down)

}
